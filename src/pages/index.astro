---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="管理画面">
  <section class="hero">
    <div class="container">
      <h1>📊 KEIBA Data Shared Admin</h1>
      <p class="lead">競馬データ共有管理画面</p>

      <div class="card-grid">
        <a href="/admin/results-manager" class="feature-card">
          <div class="card-icon">🏇</div>
          <h2>結果管理</h2>
          <p>南関公式サイトの結果を自動解析・保存</p>
          <span class="badge">主機能</span>
        </a>

        <a href="/admin/predictions-manager" class="feature-card">
          <div class="card-icon">🎯</div>
          <h2>予想管理</h2>
          <p>競馬ブックの予想を自動抽出・変換</p>
          <span class="badge">核機能</span>
        </a>

        <a href="https://github.com/apol0510/keiba-data-shared" target="_blank" class="feature-card">
          <div class="card-icon">📂</div>
          <h2>Data Repository</h2>
          <p>データリポジトリを確認</p>
        </a>

        <a href="https://github.com/apol0510/keiba-data-shared-admin" target="_blank" class="feature-card">
          <div class="card-icon">⚙️</div>
          <h2>Admin Repository</h2>
          <p>管理画面リポジトリを確認</p>
        </a>
      </div>

      <div class="info-section">
        <h2>🎯 役割分担</h2>
        <div class="roles">
          <div class="role-card">
            <h3>📊 keiba-data-shared</h3>
            <p>データ専用リポジトリ</p>
            <ul>
              <li>nankan/results/ - 結果データ</li>
              <li>nankan/predictions/ - 予想データ</li>
              <li>parser/ - パーサーライブラリ</li>
            </ul>
          </div>
          <div class="role-card">
            <h3>⚙️ keiba-data-shared-admin</h3>
            <p>管理画面専用リポジトリ</p>
            <ul>
              <li>results-manager - 結果入力画面</li>
              <li>predictions-manager - 予想変換システム（核）</li>
              <li>GitHub API - データ保存</li>
              <li>Netlify Functions - バックエンド</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </section>

  <style>
    .hero {
      padding: var(--spacing-2xl) 0;
      min-height: 80vh;
      text-align: center;
    }

    h1 {
      font-size: 3rem;
      margin-bottom: var(--spacing-md);
    }

    .lead {
      font-size: 1.5rem;
      color: var(--text-secondary);
      margin-bottom: var(--spacing-2xl);
    }

    .card-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: var(--spacing-xl);
      margin-top: var(--spacing-2xl);
    }

    .feature-card {
      padding: var(--spacing-xl);
      background: var(--bg-secondary);
      border-radius: var(--radius-lg);
      text-decoration: none;
      color: inherit;
      transition: all 0.3s ease;
      border: 2px solid transparent;
      position: relative;
    }

    .feature-card:hover {
      transform: translateY(-4px);
      border-color: var(--primary-end);
      background: var(--bg-tertiary);
    }

    .card-icon {
      font-size: 3rem;
      margin-bottom: var(--spacing-md);
    }

    .feature-card h2 {
      font-size: 1.5rem;
      margin-bottom: var(--spacing-sm);
    }

    .feature-card p {
      color: var(--text-secondary);
    }

    .badge {
      display: inline-block;
      padding: var(--spacing-xs) var(--spacing-sm);
      background: var(--primary-end);
      color: white;
      border-radius: var(--radius-sm);
      font-size: 0.875rem;
      margin-top: var(--spacing-sm);
    }

    .info-section {
      margin-top: var(--spacing-3xl);
      padding: var(--spacing-2xl);
      background: var(--bg-secondary);
      border-radius: var(--radius-lg);
    }

    .info-section h2 {
      margin-bottom: var(--spacing-xl);
    }

    .roles {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
      gap: var(--spacing-xl);
      text-align: left;
    }

    .role-card {
      padding: var(--spacing-lg);
      background: var(--bg-tertiary);
      border-radius: var(--radius-md);
    }

    .role-card h3 {
      font-size: 1.25rem;
      margin-bottom: var(--spacing-sm);
    }

    .role-card p {
      color: var(--text-secondary);
      margin-bottom: var(--spacing-md);
    }

    .role-card ul {
      list-style: none;
      padding: 0;
    }

    .role-card li {
      padding: var(--spacing-xs) 0;
      color: var(--text-secondary);
    }

    .role-card li::before {
      content: '✓ ';
      color: var(--primary-end);
      font-weight: bold;
    }
  </style>
</BaseLayout>
